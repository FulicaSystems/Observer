
set(CACHE{INTERNAL_DIR} PATH VALUE "${CMAKE_CURRENT_LIST_DIR}")

add_subdirectory(wsi)
add_subdirectory(graphics)
add_subdirectory(renderer)
# add_subdirectory(engine_types)
# add_subdirectory(host_memory)
